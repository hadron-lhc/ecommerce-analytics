-- Exercise 1: User count
-- Concepts: SELECT, COUNT(*)
SELECT COUNT(*) as amount_of_users
FROM users;

-- Exercise 2: Movie count
-- Concepts: SELECT, COUNT(*)
SELECT COUNT(*) as amount_of_movies
FROM movies;

-- Exercise 3: Session count
-- Concepts: SELECT, COUNT(*)
SELECT COUNT(*) as amount_of_sessions
FROM viewing_session;
